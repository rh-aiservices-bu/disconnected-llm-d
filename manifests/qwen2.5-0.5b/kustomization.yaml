apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# No `namespace:` field on purpose — see manifests/qwen3-4b/kustomization.yaml.

resources:
  - ../base
  - llm-inference-service.yaml
